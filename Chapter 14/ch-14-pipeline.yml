# ASP.NET
# Build and test ASP.NET projects.
# Add steps that publish symbols, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/aspnet/build-aspnet-4

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  workingDirCatalog: './Chapter 14/Catalog'
  solutionPathCatalog: './Chapter 14/Catalog/VinylStore.Catalog.API.sln'
  workingDirCart: './Chapter 14/Cart'
  solutionPathCart: './Chapter 14/Cart/VinylStore.Cart.API.sln'
steps:
- task: DotNetCoreInstaller@0
  displayName: 'Install .net core 3.0 (preview)'
  inputs:
    version: '3.0.100-preview8-013656'
- task: DotNetCoreCLI@2
  displayName: 'Building Catalog'
  inputs:
    command: 'build'
    projects: $(solutionPathCatalog)
- task: DotNetCoreCLI@2
  displayName: 'Testing Catalog'
  inputs:
    command: 'test'
    workingDirectory: $(workingDirCatalog)
- task: DotNetCoreCLI@2
  displayName: 'Building Cart'
  inputs:
    command: 'build'
    projects: $(solutionPathCart)
- task: DotNetCoreCLI@2
  displayName: 'Testing Cart'
  inputs:
    command: 'test'
    workingDirectory: $(workingDirCart)
- script: |
    docker-compose -f docker-compose.yml up -d
    docker wait catalog_api
    docker-compose -f docker-compose.yml down
  workingDirectory: $(workingDirCatalog)
  displayName: 'Docker compose build'